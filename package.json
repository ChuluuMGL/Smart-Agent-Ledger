{
  "name": "smart-agent-ledger-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Optional frontend quality tooling for the static dashboard.",
  "scripts": {
    "lint": "eslint static/*.js",
    "lint:fix": "eslint static/*.js --fix",
    "format": "prettier --write \"static/**/*.{js,css,html}\"",
    "format:check": "prettier --check \"static/**/*.{js,css,html}\""
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "prettier": "^3.0.0"
  }
}
